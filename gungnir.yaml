---
primary:
  address: ":8080"

health:
  port: ":80"
  endpoint: "/health"

log:
  file: "stdout"
  level: "INFO"
  json: true

authHeader: ["YXV0aEhlYWRlcg=="]

getLimit: 50
getRetries: 3
retryInterval: 10s
blacklistInterval: 1m

db:
  server: "db:26257"
  username: "roachadmin"
  database: "devices"
  numRetries: 4
  waitTimeMult: 5
  pingInterval: 1s
  connectTimeout: 1m
  opTimeout: 100ms

cipher:
  type: "noop"
